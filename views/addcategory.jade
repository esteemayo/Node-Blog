extends layout

block content
    if errors
        each error, i in errors
            div.alert.alert-danger #{error.msg}
    form(action="/categories/add" method="POST")
        br
        .form-group
            label(for="name") Name:
            input.form-control(name="name" id="name" type="text" autofocus)

        input.btn.btn-default(type="submit" class="btn" name="submit" value="Save →")
